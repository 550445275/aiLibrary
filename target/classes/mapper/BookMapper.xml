<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.library.mapper.BookMapper">

    <resultMap id="BookResultMap" type="Book">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="title" column="title"/>
        <result property="author" column="author"/>
        <result property="isbn" column="isbn"/>
        <result property="publishYear" column="publish_year"/>
        <result property="price" column="price"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, tenant_id, title, author, isbn, publish_year, price
    </sql>

    <select id="selectById" resultMap="BookResultMap">
        SELECT <include refid="Base_Column_List"/> FROM book
        WHERE id = #{id} AND tenant_id = #{tenantId}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM book WHERE tenant_id = #{tenantId}
    </select>

    <select id="selectPage" resultMap="BookResultMap">
        SELECT <include refid="Base_Column_List"/> FROM book
        WHERE tenant_id = #{tenantId}
        ORDER BY id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO book (tenant_id, title, author, isbn, publish_year, price)
        VALUES (#{tenantId}, #{title}, #{author}, #{isbn}, #{publishYear}, #{price})
    </insert>

    <update id="updateById">
        UPDATE book
        SET title = #{title},
            author = #{author},
            isbn = #{isbn},
            publish_year = #{publishYear},
            price = #{price}
        WHERE id = #{id} AND tenant_id = #{tenantId}
    </update>

    <delete id="deleteById">
        DELETE FROM book WHERE id = #{id} AND tenant_id = #{tenantId}
    </delete>
</mapper>
