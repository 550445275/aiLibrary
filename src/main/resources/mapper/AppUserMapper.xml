<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.library.mapper.AppUserMapper">

    <resultMap id="AppUserResultMap" type="AppUser">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="enabled" column="enabled"/>
        <result property="platformAdmin" column="platform_admin"/>
    </resultMap>

    <select id="countUsersByTenant" resultType="long">
        SELECT COUNT(*) FROM app_users WHERE tenant_id = #{tenantId}
    </select>

    <select id="selectByTenantAndUsername" resultMap="AppUserResultMap">
        SELECT id, tenant_id, username, password, role, enabled, platform_admin
        FROM app_users
        WHERE tenant_id = #{tenantId} AND username = #{username}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="AppUserResultMap">
        SELECT id, tenant_id, username, password, role, enabled, platform_admin
        FROM app_users
        WHERE id = #{id}
    </select>

    <select id="listByTenantId" resultMap="AppUserResultMap">
        SELECT id, tenant_id, username, password, role, enabled, platform_admin
        FROM app_users
        WHERE tenant_id = #{tenantId}
        ORDER BY id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO app_users (tenant_id, username, password, role, enabled, platform_admin)
        VALUES (#{tenantId}, #{username}, #{password}, #{role}, #{enabled}, #{platformAdmin})
    </insert>

    <update id="updateForPlatform">
        UPDATE app_users
        SET role = #{role},
            enabled = #{enabled},
            platform_admin = #{platformAdmin}
        WHERE id = #{id} AND tenant_id = #{tenantId}
    </update>
</mapper>
